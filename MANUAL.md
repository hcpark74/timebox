# Timebox Flow 사용자 매뉴얼

Timebox Flow는 하루 할 일을 정리하고, 시간대별로 배정해 타임라인으로 확인할 수 있는 간단한 스케줄 관리 앱입니다.

## 1. 화면 구성

앱에는 현재 다음 화면이 구현되어 있습니다.

### Timeline

- 선택한 날짜의 스케줄을 시간대별로 확인합니다.
- 각 태스크의 체크박스를 눌러 `Pending` / `Completed` 상태를 바꿀 수 있습니다.
- 상단 달력 아이콘을 누르면 `Calendar` 화면으로 이동합니다.

### Calendar

- 주간 날짜 목록이 표시됩니다.
- 날짜를 누르면 해당 날짜 기준으로 일정 목록이 바뀝니다.
- 해당 날짜에 스케줄된 태스크가 없으면 빈 상태 메시지가 표시됩니다.

### Schedule Task

- 태스크를 `Today's Priorities`와 `Backlog`로 나누어 볼 수 있습니다.
- 입력창에 제목을 입력하고 `Add` 버튼을 누르면 새 태스크가 추가됩니다.
- 휴지통 아이콘으로 태스크를 삭제할 수 있습니다.
- 드래그 앤 드롭으로 `Priorities`와 `Backlog` 사이를 이동할 수 있습니다.

### Schedule Your Task

- 아직 시간 배정이 안 된 태스크를 시간 슬롯에 드롭해서 스케줄링합니다.
- 오른쪽 시간 슬롯은 08:00부터 20:30까지 제공됩니다.
- `Re-Arrange`를 누르면 현재 선택된 날짜의 배정이 초기화됩니다.
- `Schedule`을 누르면 `Timeline` 화면으로 이동해 결과를 볼 수 있습니다.

### Profile

- 사용자 인사말과 Sync ID를 확인할 수 있습니다.
- Sync ID를 눌러 복사할 수 있습니다.
- 편집 아이콘으로 Sync ID를 변경할 수 있습니다.
- 카테고리별 태스크 개수를 확인할 수 있습니다.

## 2. 주요 사용 방법

### 새 태스크 추가

1. `Schedule Task` 화면으로 이동합니다.
2. 입력창에 할 일 제목을 입력합니다.
3. `Add` 버튼을 누릅니다.

새 태스크는 기본적으로 `Backlog`에 추가됩니다.

### 우선순위 변경

1. `Schedule Task` 화면에서 태스크를 길게 드래그합니다.
2. `Today's Priorities` 또는 `Backlog` 영역으로 옮깁니다.

`Today's Priorities`에 들어가면 높은 우선순위로 처리됩니다.

### 시간 배정

1. `Schedule Task` 화면에서 `Let's Schedule` 버튼을 누릅니다.
2. 왼쪽 태스크 목록에서 원하는 태스크를 드래그합니다.
3. 오른쪽 원하는 시간 슬롯에 놓습니다.
4. `Schedule` 버튼을 눌러 타임라인에서 확인합니다.

같은 시간 슬롯에 다른 태스크를 놓으면 기존 태스크의 시간 배정은 해제됩니다.

### 완료 처리

1. `Timeline` 화면에서 태스크 블록을 찾습니다.
2. 체크박스를 눌러 완료 상태를 바꿉니다.

완료 상태는 저장되며 다시 열어도 유지됩니다.

### Sync ID 사용

1. `Profile` 화면으로 이동합니다.
2. 표시된 Sync ID를 복사하거나 편집합니다.
3. 같은 Sync ID를 사용하는 환경에서는 Cloudflare KV가 설정된 경우 같은 데이터를 불러올 수 있습니다.

## 3. 데이터 저장

- 기본 데이터는 브라우저 `localStorage`에 저장됩니다.
- 네트워크 환경과 Cloudflare 설정이 준비되어 있으면 Sync ID 기반 동기화를 사용할 수 있습니다.
- 브라우저 저장소를 삭제하면 로컬 데이터도 함께 삭제됩니다.

## 4. 오프라인 사용

- 서비스 워커가 기본 앱 파일을 캐시합니다.
- 한 번 로드한 뒤에는 오프라인에서도 기본 UI를 다시 열 수 있습니다.
- 다만 API 동기화는 네트워크가 필요합니다.

## 5. 참고 사항

- 하단의 `Search` 탭은 현재 동작하지 않는 UI입니다.
- 통계 API 파일은 존재하지만, 별도의 통계 화면은 아직 구현되지 않았습니다.
